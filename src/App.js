import { ShellBar, ThemeProvider } from "@ui5/webcomponents-react";

function App() {
  return (
    <ThemeProvider>
      <ShellBar title="Irvine Company Apartments" />
    </ThemeProvider>
  );
}

export default App;
